/**
 * let: The let statement declares a block-scoped variable,
 * optionally initializing it to a value.
 */
{
let a = 5;
console.log(a);
}


/**
 * let: The var statement declares a functional-scoped variable,
 * optionally initializing it to a value.
 */

var b = 13;
console.log(b);