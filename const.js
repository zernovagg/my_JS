/**
 * const: Constants are block-scooped, much like variables declared using let keywrod.
 * The value of constant can't be changed through reassignment, and it can't be redeclared.
 */

const a = 5;
console.log(a);